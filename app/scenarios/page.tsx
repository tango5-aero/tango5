import { TextareaForm } from '~/components/scenario-uploader';

export default function App() {
    return (
        <main className="flex min-h-svh flex-col items-center justify-center gap-6 bg-muted p-6 md:p-10">
            <TextareaForm />
        </main>
    );
}
